地址分布
00004000~00007000 保存系统变量
00007c00~00008000 mbr 先启动然后移动到4000~4200处
00007c00~00008000 boot 读取loader
00008000~00009000 loader 读取设置系统信息,加载setup与kernel,设置好idt与gdt,切换到保护模式
					loader的16位栈也在其中，初始化简单的idt与gdt 
00009000~0000a000 setup 进行内存分页，将内核地址映射到3G以上，
                        结束后得到物理内存分页信息放到系统变量中，
                        对kernel设置虚拟内存设置，也放到其中
00010000~000xxxxx kernel由于raw中设计填充0所以rawaddress = virtualaddress rsize>=vsize 直接读取就行
c0010000~ffffffff kernel 内存
ffffe000~fffffffc 先分配一个页给内核做栈，不够再加