使用vhd虚拟硬盘，分别使用O和P盘符，如果有冲突请修改各根目录下makefile中盘符和MakeVHD中的盘符
在目录下如果没有发现myos.vhd会自动生成一个
生成过程会有三次(如果已有myos.vhd就会是两次)请求权限，都是diskpart处理vhd

klib.lib是仅限于内核使用，不能用于测试

地址分布
00001000~00001200       用作存放DBR
00001200~00002000       用于存放目录
00002000~00009000       用于其他读取
00002000~00003000       用一个页来存放系统数据，这个页暂时设置为占用，等值都取出来后再释放掉
00002000~00002200       DBR
00002200~00002300       显卡相关数据
00002300~00002400       存放有关内存管理的内容
00002400~00002500       存放
00003000~00003200       读取显卡信息用的temp
00004000~00004800       基础的字库 boot.fnt
00005000~00005800       gdt
00005800~00006000       idt

00018000~00020000 setup 临时的栈
00020000~00030000 bootmgr windows生成的虚拟硬盘mbr启动活动扇区的dbr。
                            dbr在当前扇区的根目录下寻找bootmgr然后运行之
                            自带的dbr有一些可以用的参数和内存，但为了
                            实现可移植，所以bootmgr从头读起，反正不在乎内存了
                            读取kernel，用于设置idt、gdt，设置显卡，进入保护模式，跳转到setup，

00030000~00005000 setup 将kernel加载到对应内存中并且映射内存，然后进入kernel
00050000~00100000 kernel暂存
00100000~00xxxxxx kernel 
c0100000~ffffffff kernel 内存
ffffe000~fffffffc 先分配一个页给内核做栈，不够再加