#include <types.h>
#include <fs/pe.h>
#include "setup.h"

void main() {
	/*DosHeader* kernelHeader = (DosHeader*)KernelTempBase;
	NtHeader* ntHeader = (NtHeader*)(KernelTempBase + kernelHeader->lfanew);*/
	for (;;);
}